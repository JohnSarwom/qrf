import type { Project } from "./types";

export const projects: Project[] = [];

export const projectPublicationNotice =
  "Project names, client logos, photographs, locations, counts and outcomes are published only after written approval.";

